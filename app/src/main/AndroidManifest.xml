<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.mycashbook.RegisterActivity"></activity>
        <activity android:name="com.example.mycashbook.CashFlowActivity" />
        <activity android:name="com.example.mycashbook.SettingsActivity" />
        <activity android:name="com.example.mycashbook.PengeluaranActivity" />
        <activity android:name="com.example.mycashbook.PemasukanActivity" />
        <activity android:name="com.example.mycashbook.HomeActivity" />
        <activity android:name="com.example.mycashbook.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>